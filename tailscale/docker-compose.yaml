services:
    tailscale:
        image: tailscale/tailscale:latest
        container_name: tailscale
        hostname: homeserver
        restart: unless-stopped
        environment:
            - TS_STATE_DIR=/var/lib/tailscale
            - TS_AUTHKEY=${TS_AUTHKEY}
        volumes:
            - ${TS_STATE_DIR}:/var/lib/tailscale
            - ${TS_CERTS_DIR}:/var/lib/tailscale/certs
            - /dev/net/tun:/dev/net/tun
        cap_add:
            - NET_ADMIN
            - SYS_MODULE
        network_mode: host

#networks:
#  proxy:
#    external: true
