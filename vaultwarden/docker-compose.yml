services:
    vaultwarden:
        image: vaultwarden/server:latest
        container_name: vaultwarden
        environment:
            - DOMAIN=${VW_DOMAIN}
        volumes:
            - ${VW_DATA_DIR}:/data/
        restart: unless-stopped
        networks:
            - proxy

networks:
    proxy:
        external: true
